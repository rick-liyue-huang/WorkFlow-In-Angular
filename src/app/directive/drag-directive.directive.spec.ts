import { DragDirectiveDirective } from './drag-directive.directive';

describe('DragDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
