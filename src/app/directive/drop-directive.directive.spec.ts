import { DropDirectiveDirective } from './drop-directive.directive';

describe('DropDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
